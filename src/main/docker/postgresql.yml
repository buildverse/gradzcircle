version: '2'
services:
    gradzcircle-postgresql:
        image: postgres:10.4
        # volumes:
        #     - ~/volumes/jhipster/gradzcircle/postgresql/:/var/lib/postgresql/data/
        environment:
            - POSTGRES_USER=gradzcircle
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
